import template from './lockedPage.html';
import controller from './lockedPage.controller';
import './lockedPage.scss';

let lockedPageComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default lockedPageComponent;
